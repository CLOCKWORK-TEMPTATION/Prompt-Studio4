# Prompt Studio - نظام تحسين البرومبتات

## 🎯 نظرة عامة

Prompt Studio هو تطبيق متقدم لهندسة وتحسين البرومبتات (Prompts) مع دعم كامل للغة العربية. يوفر أدوات شاملة لإنشاء واختبار وتحسين المطالبات المستخدمة مع نماذج الذكاء الاصطناعي.

## ✨ الميزات الجديدة المُضافة

### 1. 🔢 عداد التوكنات والتكلفة (Token Counter & Cost Estimator)
- **حساب فوري**: يعرض عدد التوكنات المقدرة لكل قسم من أقسام البرومبت
- **تقدير التكلفة**: يحسب التكلفة المتوقعة بناءً على النموذج المستخدم
- **دعم نماذج متعددة**: GPT-4o، Claude 3.5 Sonnet، Gemini Pro، وغيرها
- **تحسين للغة العربية**: يأخذ في الاعتبار أن النص العربي يستهلك توكنات أكثر

**الموقع**: شريط الأدوات العلوي في صفحة Studio

### 2. 📚 نظام تتبع الإصدارات (Version History)
- **حفظ تلقائي**: يحفظ نسخة جديدة تلقائياً بعد كل تشغيل ناجح
- **حفظ يدوي**: إمكانية حفظ النسخة الحالية مع تسمية مخصصة
- **استرجاع سهل**: عرض سجل الإصدارات مع إمكانية استرجاع أي نسخة سابقة
- **تنظيم ذكي**: يحفظ آخر 50 نسخة فقط لتجنب استهلاك التخزين
- **تفاصيل كاملة**: يحفظ جميع الأقسام والمتغيرات مع كل نسخة

**الموقع**: زر "السجل" في شريط الأدوات العلوي

### 3. 🎯 نظام نقد محسّن (Enhanced Critique System)
يقدم تحليلاً شاملاً لجودة البرومبت مع مقاييس متقدمة:

#### المقاييس الأربعة:
- **الوضوح (Clarity)**: مدى وضوح التعليمات
- **التحديد (Specificity)**: مدى تحديد المطلوب بدقة
- **البنية (Structure)**: جودة تنظيم الأقسام
- **الأمثلة (Examples)**: وجود أمثلة توضيحية (Few-Shot)

#### الكشف التلقائي عن المشاكل:
- غياب دور النظام (System Role)
- أخطاء في صيغة المتغيرات
- استخدام لغة غامضة
- عدم تحديد تنسيق الإخراج
- نقص في الأمثلة التوضيحية
- مطالبات قصيرة جداً

#### الإصلاح التلقائي:
- بعض المشاكل يمكن إصلاحها تلقائياً بنقرة واحدة
- عرض مقترحات تحسين قابلة للتطبيق

**الموقع**: تبويب "النقد والتحسين" في اللوحة اليمنى

### 4. 📊 إحصائيات التشغيل المفصلة (Detailed Run Statistics)
عند تشغيل البرومبت، يعرض:
- عدد التوكنات المستهلكة (Input + Output)
- التكلفة الفعلية
- مدة التنفيذ بالثواني
- النموذج المستخدم

**الموقع**: أعلى نافذة المخرجات (Output)

### 5. 🆚 مقارنة A/B للبرومبتات (A/B Testing)
صفحة جديدة كاملة لمقارنة إصدارين من البرومبت جنباً إلى جنب:
- **تشغيل متزامن**: تشغيل الإصدارين معاً في نفس الوقت
- **مقارنة النتائج**: عرض المخرجات والإحصائيات جنباً إلى جنب
- **تحديد الفائز**: يحدد تلقائياً الإصدار الأفضل بناءً على الكفاءة
- **تعديل مستقل**: تحرير كل إصدار بشكل منفصل

**الموقع**: صفحة "مقارنة A/B" في القائمة الجانبية

### 6. 📚 مكتبة قوالب موسعة (Expanded Templates)
تمت إضافة 4 قوالب جديدة:
- **مُحلل البيانات**: لتحليل البيانات واستخراج الرؤى
- **مُترجم تقني**: للترجمة الاحترافية للمستندات التقنية
- **مُولد اختبارات البرمجة**: لإنشاء Unit Tests تلقائياً
- **مُحسّن SEO**: لتحسين المحتوى لمحركات البحث

**الموقع**: صفحة "القوالب"

### 7. 🎓 تقنيات جديدة (New Prompting Techniques)
تمت إضافة 4 تقنيات جديدة:
- **Role Prompting**: تحديد دور واضح للنموذج
- **Output Format Specification**: تحديد تنسيق الإخراج
- **Constraint-Based Prompting**: وضع قيود واضحة
- **Negative Prompting**: توضيح ما لا تريده

**الموقع**: صفحة "التقنيات"

## 🛠️ التحسينات التقنية

### أنواع البيانات المحسّنة:
```typescript
// دعم أنواع المتغيرات
interface Variable {
  type?: 'string' | 'number' | 'array' | 'object';
  description?: string;
}

// معلومات مفصلة عن التوكنات
interface RunResult {
  tokens?: {
    prompt: number;
    completion: number;
    total: number;
  };
  cost?: number;
  duration?: number;
}

// مقاييس النقد المتقدمة
interface CritiqueResult {
  metrics?: {
    clarity: number;
    specificity: number;
    structure: number;
    examples: number;
  };
}
```

### ملفات الأدوات الجديدة:
- `token-utils.ts`: حساب التوكنات والتكلفة
- `version-utils.ts`: إدارة الإصدارات والسجل

## 📈 استخدام الميزات الجديدة

### 1. تتبع التكلفة:
```
1. افتح صفحة Studio
2. اكتب البرومبت الخاص بك
3. شاهد عدد التوكنات والتكلفة المقدرة في الشريط العلوي
4. قارن التكاليف بين النماذج المختلفة
```

### 2. حفظ واسترجاع الإصدارات:
```
1. اضغط على زر الحفظ (💾) لحفظ النسخة الحالية
2. اضغط على "السجل" لعرض الإصدارات السابقة
3. انقر على أي إصدار لاسترجاعه
```

### 3. تحليل جودة البرومبت:
```
1. اضغط على "نقد (Critique)"
2. راجع المقاييس الأربعة والنتيجة الإجمالية
3. اقرأ المشاكل المكتشفة
4. اضغط "تطبيق تلقائي" للمشاكل القابلة للإصلاح
```

### 4. مقارنة A/B:
```
1. افتح صفحة "مقارنة A/B"
2. اكتب الإصدار A في اليسار
3. اكتب الإصدار B في اليمين
4. اضغط "تشغيل كلاهما"
5. قارن النتائج والإحصائيات
```

## 🎨 واجهة المستخدم

### التحسينات البصرية:
- **مؤشرات ملونة**: ألوان مختلفة لمستويات الخطورة (أحمر، أصفر، أزرق)
- **أشرطة تقدم**: مقاييس مرئية لجودة البرومبت
- **شارات**: عرض المعلومات المهمة بشكل بارز
- **رموز تعبيرية**: 🏆 للفائز في مقارنة A/B

## 🚀 الميزات المستقبلية (قيد التطوير)

- [ ] تصدير واستيراد القوالب (JSON/YAML)
- [ ] مشاركة البرومبتات مع الفريق
- [ ] لوحة تحليلات الأداء
- [ ] دعم المزيد من النماذج
- [ ] اختبارات تلقائية للبرومبتات
- [ ] تحسين AI للبرومبتات

## 📝 ملاحظات

- جميع البيانات تُحفظ في `localStorage` في المتصفح
- النظام يستخدم واجهة محاكاة (Mock API) حالياً
- للإنتاج: قم بربط النظام بـ API حقيقية (OpenAI، Anthropic، إلخ)

## 🤝 المساهمة

هذا المشروع مفتوح المصدر ويرحب بالمساهمات!

---

**النسخة**: 1.0.0  
**اللغات المدعومة**: العربية، الإنجليزية  
**الترخيص**: MIT
