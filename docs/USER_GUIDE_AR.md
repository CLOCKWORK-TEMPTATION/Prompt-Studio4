# 📖 دليل المستخدم الشامل - Prompt Studio 4

<div dir="rtl">

## المحتويات

1. [مقدمة](#مقدمة)
2. [البدء السريع](#البدء-السريع)
3. [الواجهة الرئيسية](#الواجهة-الرئيسية)
4. [سير العمل السبع مراحل](#سير-العمل-السبع-مراحل)
5. [نظام الوكلاء الثلاثة](#نظام-الوكلاء-الثلاثة)
6. [إدارة القوالب والتقنيات](#إدارة-القوالب-والتقنيات)
7. [التعاون الحي](#التعاون-الحي)
8. [توليد SDK](#توليد-sdk)
9. [التخزين المؤقت الدلالي](#التخزين-المؤقت-الدلالي)
10. [النشر السحابي](#النشر-السحابي)
11. [الإعدادات](#الإعدادات)
12. [أمثلة عملية](#أمثلة-عملية)
13. [استكشاف الأخطاء](#استكشاف-الأخطاء)
14. [الأسئلة الشائعة](#الأسئلة-الشائعة)

---

## مقدمة

**Prompt Studio 4** هو منصة متقدمة لهندسة المطالبات (Prompt Engineering) تساعدك على تصميم وتحسين وإدارة المطالبات بطريقة احترافية ومنظمة.

### الميزات الرئيسية

| الميزة | الوصف |
|--------|--------|
| 🤖 **نظام الوكلاء الثلاثة** | تحسين تلقائي للمطالبات باستخدام ثلاثة وكلاء ذكاء اصطناعي |
| 📊 **سير العمل المنظم** | 7 مراحل واضحة من الفكرة إلى التنظيم |
| 👥 **التعاون الحي** | العمل الجماعي في الوقت الفعلي مع فريقك |
| 🔧 **توليد SDK** | توليد كود جاهز للاستخدام بـ 5 لغات برمجة |
| 💾 **التخزين الدلالي** | توفير التكاليف عبر التخزين المؤقت الذكي |
| ☁️ **النشر السحابي** | نشر مباشر على AWS, GCP, Vercel, Cloudflare |

---

## البدء السريع

### المتطلبات الأساسية

```bash
# Node.js 18 أو أحدث
node --version  # v18.0.0+

# PostgreSQL 14 أو أحدث
psql --version  # 14.0+

# Redis (اختياري للتخزين المؤقت)
redis-server --version
```

### التثبيت

```bash
# استنساخ المشروع
git clone https://github.com/your-org/prompt-studio4.git
cd prompt-studio4

# تثبيت المكتبات
npm install

# إعداد قاعدة البيانات
npm run db:push

# تشغيل التطبيق
npm run dev
```

### الوصول للتطبيق

بعد التشغيل، افتح المتصفح على:
- **الواجهة الأمامية**: `http://localhost:5000`
- **API**: `http://localhost:5000/api`

---

## الواجهة الرئيسية

### شريط التنقل

```
┌─────────────────────────────────────────────────────────────┐
│  🏠 الرئيسية  │  🎨 الاستوديو  │  📚 القوالب  │  🔧 التقنيات  │
│  📊 السجل    │  👥 التعاون   │  ⚙️ الإعدادات              │
└─────────────────────────────────────────────────────────────┘
```

### الصفحات الرئيسية

| الصفحة | المسار | الوظيفة |
|--------|--------|--------|
| الرئيسية | `/` | لوحة التحكم والإحصائيات |
| الاستوديو | `/studio` | إنشاء وتحرير المطالبات |
| القوالب | `/templates` | إدارة قوالب المطالبات |
| التقنيات | `/techniques` | تقنيات تحسين المطالبات |
| السجل | `/runs` | سجل تشغيل المطالبات |
| التعاون | `/collaboration` | جلسات التعاون الحي |
| الإعدادات | `/settings` | إعدادات التطبيق |

---

## سير العمل السبع مراحل

يتبع Prompt Studio سير عمل منظم من 7 مراحل لضمان جودة المطالبات:

### 📋 نظرة عامة

```
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│ المرحلة 0│───▶│ المرحلة 1│───▶│ المرحلة 2│───▶│ المرحلة 3│
│ الفكرة   │    │ التكوين  │    │ المراجعة │    │ التحرير  │
└──────────┘    └──────────┘    └──────────┘    └──────────┘
                                                      │
┌──────────┐    ┌──────────┐    ┌──────────┐         │
│ المرحلة 6│◀───│ المرحلة 5│◀───│ المرحلة 4│◀────────┘
│ التنظيم  │    │ التنفيذ  │    │ التحليل  │
└──────────┘    └──────────┘    └──────────┘
```

### المرحلة 0: الفكرة الخام 💡

**الهدف**: إدخال الفكرة الأولية للمطالبة

**كيفية الاستخدام**:
1. اكتب فكرتك بشكل حر في مربع النص
2. لا تقلق بشأن التنسيق أو البنية
3. اضغط "التالي" للانتقال للمرحلة التالية

**مثال**:
```
أريد مطالبة تساعدني في كتابة ملخصات تنفيذية
للتقارير المالية بطريقة احترافية ومختصرة
```

---

### المرحلة 1: التكوين الثلاثي 🤖

**الهدف**: تحويل الفكرة إلى مطالبة منظمة باستخدام 3 وكلاء

**العملية**:
```
فكرتك ──▶ [الوكيل 1: المُحول] ──▶ مطالبة منظمة
                                        │
              ◀── [الوكيل 2: الناقد] ◀──┘
                        │
              ──▶ [الوكيل 3: الحكم] ──▶ المطالبة النهائية
```

**النتيجة**: مطالبة مُقسمة إلى 4 أقسام:
- **System Prompt**: تعريف دور النموذج
- **Developer Instructions**: تعليمات للمطور
- **User Message**: رسالة المستخدم
- **Context**: السياق والمعلومات الإضافية

---

### المرحلة 2: المراجعة ✅

**الهدف**: مراجعة نتائج الوكلاء واختيار الأفضل

**الخيارات المتاحة**:
- ✅ **قبول**: الموافقة على النتيجة الحالية
- 🔄 **إعادة التوليد**: طلب نتيجة جديدة
- ✏️ **تعديل يدوي**: الانتقال للمرحلة التالية للتحرير

---

### المرحلة 3: التحرير المتقدم ✏️

**الهدف**: تحرير دقيق لكل قسم من أقسام المطالبة

**واجهة التحرير**:
```
┌─────────────────────────────────────────────────┐
│ System Prompt                                    │
├─────────────────────────────────────────────────┤
│ أنت محلل مالي خبير متخصص في كتابة الملخصات    │
│ التنفيذية. تتميز بالدقة والاختصار...           │
│                                                  │
│ [📝 تحرير] [🔄 إعادة توليد] [📋 نسخ]           │
└─────────────────────────────────────────────────┘
```

**أدوات التحرير**:
- تحرير نصي مباشر
- إدراج متغيرات `{{variable_name}}`
- استخدام قوالب جاهزة
- تطبيق تقنيات تحسين

---

### المرحلة 4: تحليل الجودة 📊

**الهدف**: الحصول على تقييم موضوعي للمطالبة

**معايير التقييم**:

| المعيار | الوصف | النطاق |
|---------|--------|--------|
| الوضوح | مدى وضوح التعليمات | 1-10 |
| الاكتمال | شمولية المطالبة | 1-10 |
| البنية | جودة التنظيم | 1-10 |
| الفعالية | المتوقع من النتيجة | 1-10 |

**مثال على التقييم**:
```json
{
  "overall_score": 8.5,
  "clarity": 9,
  "completeness": 8,
  "structure": 9,
  "effectiveness": 8,
  "suggestions": [
    "إضافة أمثلة على التنسيق المطلوب",
    "تحديد طول الملخص المتوقع"
  ]
}
```

---

### المرحلة 5: التنفيذ ▶️

**الهدف**: تشغيل المطالبة على نموذج LLM

**خيارات التنفيذ**:
```
┌─────────────────────────────────────┐
│ اختيار النموذج                      │
│ ○ Llama 3.3 70B (افتراضي)          │
│ ○ GPT-4                             │
│ ○ Claude 3                          │
├─────────────────────────────────────┤
│ إعدادات التنفيذ                     │
│ Temperature: [0.7    ▼]             │
│ Max Tokens: [2048   ▼]              │
├─────────────────────────────────────┤
│        [▶️ تنفيذ المطالبة]          │
└─────────────────────────────────────┘
```

**النتيجة**: عرض استجابة النموذج مع:
- زمن الاستجابة
- عدد الرموز المستخدمة
- التكلفة التقديرية
- خيار الحفظ في السجل

---

### المرحلة 6: التنظيم 📁

**الهدف**: حفظ المطالبة للاستخدام المستقبلي

**خيارات الحفظ**:

1. **حفظ كقالب جديد**:
   ```
   الاسم: ملخص تنفيذي مالي
   الوصف: قالب لإنشاء ملخصات تنفيذية للتقارير المالية
   الفئة: الأعمال > التقارير
   ```

2. **تحديث قالب موجود**:
   - اختيار القالب من القائمة
   - حفظ كإصدار جديد

3. **حفظ كتقنية**:
   - استخراج التقنية المستخدمة
   - إضافتها لمكتبة التقنيات

---

## نظام الوكلاء الثلاثة

### الوكيل الأول: المُحول 🔄

**الدور**: تحويل الفكرة الخام إلى مطالبة منظمة

**المدخلات**:
```
فكرة المستخدم الخام
```

**المخرجات**:
```json
{
  "systemPrompt": "...",
  "developerInstructions": "...",
  "userMessage": "...",
  "context": "..."
}
```

**مثال عملي**:

*المدخل*:
```
أريد كتابة ايميلات تسويقية
```

*المخرج*:
```json
{
  "systemPrompt": "أنت خبير تسويق محترف متخصص في كتابة رسائل البريد الإلكتروني التسويقية. تتميز بأسلوب جذاب ومقنع يحقق معدلات تحويل عالية.",
  "developerInstructions": "- استخدم عناوين جذابة\n- اجعل الرسالة مختصرة\n- ضمّن دعوة واضحة للعمل",
  "userMessage": "اكتب بريداً إلكترونياً تسويقياً لـ {{product}} موجهاً لـ {{target_audience}}",
  "context": "نوع المنتج: {{product_type}}\nالعرض: {{offer}}"
}
```

---

### الوكيل الثاني: الناقد 🔍

**الدور**: تحليل ونقد المطالبة وتقديم تحسينات

**التقييم يشمل**:
- نقاط القوة
- نقاط الضعف
- اقتراحات محددة
- مطالبة بديلة محسّنة

**مثال على النقد**:
```markdown
## نقاط القوة
- تعريف واضح للدور
- استخدام متغيرات للتخصيص

## نقاط الضعف
- غياب أمثلة على الأسلوب المطلوب
- عدم تحديد طول الرسالة

## اقتراحات
1. إضافة قسم للأمثلة
2. تحديد نطاق 150-250 كلمة
3. إضافة نبرة العلامة التجارية
```

---

### الوكيل الثالث: الحكم ⚖️

**الدور**: دمج الآراء واتخاذ القرار النهائي

**العملية**:
1. مراجعة المطالبة الأصلية
2. تحليل نقد الوكيل الثاني
3. تطبيق التحسينات المناسبة
4. إنتاج المطالبة النهائية

**المخرجات**:
```json
{
  "finalPrompt": {
    "systemPrompt": "...",
    "developerInstructions": "...",
    "userMessage": "...",
    "context": "..."
  },
  "appliedFixes": [
    "إضافة أمثلة على الأسلوب",
    "تحديد طول الرسالة",
    "توضيح نبرة العلامة التجارية"
  ],
  "finalVariables": ["product", "target_audience", "product_type", "offer", "brand_tone"]
}
```

---

## إدارة القوالب والتقنيات

### القوالب 📚

**إنشاء قالب جديد**:
1. اذهب إلى `/templates`
2. اضغط "قالب جديد"
3. املأ البيانات:

```
┌─────────────────────────────────────────────────┐
│ إنشاء قالب جديد                                 │
├─────────────────────────────────────────────────┤
│ الاسم: [____________________________]           │
│ الوصف: [____________________________]           │
│ الفئة: [الأعمال              ▼]                │
│                                                  │
│ System Prompt:                                  │
│ ┌───────────────────────────────────────────┐  │
│ │                                           │  │
│ └───────────────────────────────────────────┘  │
│                                                  │
│ المتغيرات: {{var1}}, {{var2}}                  │
│                                                  │
│        [حفظ القالب]    [إلغاء]                 │
└─────────────────────────────────────────────────┘
```

**استخدام قالب**:
1. اختر القالب من القائمة
2. املأ قيم المتغيرات
3. اضغط "استخدام في الاستوديو"

---

### التقنيات 🔧

**التقنيات المتاحة**:

| التقنية | الوصف | مثال |
|---------|--------|-------|
| Chain of Thought | التفكير خطوة بخطوة | "فكر خطوة بخطوة..." |
| Few-Shot | تقديم أمثلة | "مثال 1:... مثال 2:..." |
| Role Play | تعيين دور محدد | "أنت خبير في..." |
| Structured Output | تحديد التنسيق | "أجب بصيغة JSON..." |
| Self-Consistency | التحقق الذاتي | "راجع إجابتك وتأكد..." |

**تطبيق تقنية**:
1. اذهب إلى المرحلة 3 (التحرير)
2. اختر "تطبيق تقنية"
3. حدد التقنية المطلوبة
4. التقنية ستُدمج تلقائياً في المطالبة

---

## التعاون الحي

### إنشاء جلسة تعاون 👥

```bash
# الخطوات:
1. اذهب إلى /collaboration
2. اضغط "جلسة جديدة"
3. شارك رابط الجلسة مع فريقك
4. ابدأ التحرير معاً!
```

### واجهة التعاون

```
┌─────────────────────────────────────────────────────────────┐
│ 🟢 جلسة: تحسين مطالبة التسويق     [👥 3 متصلين] [🔗 مشاركة]│
├─────────────────────────────────────────────────────────────┤
│ المشاركون:                                                  │
│ 🟢 أحمد (أنت) - يحرر System Prompt                         │
│ 🟢 سارة - تشاهد                                            │
│ 🟢 محمد - يحرر Context                                     │
├─────────────────────────────────────────────────────────────┤
│ [المحرر المشترك مع مؤشرات الحضور]                          │
│ ┌───────────────────────────────────────────────────────┐  │
│ │ أنت خبير تسويق... |أحمد                              │  │
│ │                    |سارة                              │  │
│ └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

### ميزات التعاون

- **التحرير المتزامن**: عدة مستخدمين يحررون نفس المستند
- **مؤشرات الحضور**: رؤية موقع مؤشر كل مستخدم
- **ألوان مميزة**: كل مستخدم بلون مختلف
- **سجل التعديلات**: تتبع جميع التغييرات
- **حل النزاعات**: نظام CRDT لمنع التعارضات

---

## توليد SDK

### اللغات المدعومة 💻

```
┌────────────────────────────────────────────────────────────┐
│ اختر اللغة المطلوبة:                                       │
├────────────────────────────────────────────────────────────┤
│ ○ TypeScript  - مع Async/Await وأنواع قوية               │
│ ○ Python      - مع Type Hints و Requests                 │
│ ○ JavaScript  - مع Axios و Promises                      │
│ ○ Go          - مع HTTP Client                           │
│ ○ cURL/Bash   - سكريبتات Shell                           │
└────────────────────────────────────────────────────────────┘
```

### مثال: توليد SDK لـ TypeScript

**الخطوات**:
1. اذهب إلى `/sdk-generator`
2. اختر المطالبة أو القالب
3. حدد "TypeScript"
4. اضغط "توليد"

**النتيجة**:

```typescript
// prompt-studio-sdk.ts

interface PromptVariables {
  product: string;
  targetAudience: string;
  productType: string;
  offer: string;
}

interface PromptResponse {
  content: string;
  tokens: number;
  latency: number;
}

export async function generateMarketingEmail(
  variables: PromptVariables
): Promise<PromptResponse> {
  const response = await fetch('/api/ai/run', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
    },
    body: JSON.stringify({
      systemPrompt: `أنت خبير تسويق محترف...`,
      userMessage: `اكتب بريداً إلكترونياً تسويقياً لـ ${variables.product}...`,
      context: `نوع المنتج: ${variables.productType}...`,
    }),
  });

  return response.json();
}

// استخدام الـ SDK
const result = await generateMarketingEmail({
  product: 'تطبيق إدارة المهام',
  targetAudience: 'رواد الأعمال',
  productType: 'SaaS',
  offer: 'خصم 50% لأول 3 أشهر',
});

console.log(result.content);
```

### مثال: توليد SDK لـ Python

```python
# prompt_studio_sdk.py

from typing import TypedDict
import requests

class PromptVariables(TypedDict):
    product: str
    target_audience: str
    product_type: str
    offer: str

class PromptResponse(TypedDict):
    content: str
    tokens: int
    latency: float

def generate_marketing_email(variables: PromptVariables) -> PromptResponse:
    """توليد بريد إلكتروني تسويقي"""
    response = requests.post(
        'http://localhost:5000/api/ai/run',
        json={
            'systemPrompt': 'أنت خبير تسويق محترف...',
            'userMessage': f'اكتب بريداً إلكترونياً تسويقياً لـ {variables["product"]}...',
            'context': f'نوع المنتج: {variables["product_type"]}...',
        }
    )
    return response.json()

# استخدام الـ SDK
if __name__ == '__main__':
    result = generate_marketing_email({
        'product': 'تطبيق إدارة المهام',
        'target_audience': 'رواد الأعمال',
        'product_type': 'SaaS',
        'offer': 'خصم 50% لأول 3 أشهر',
    })
    print(result['content'])
```

---

## التخزين المؤقت الدلالي

### كيف يعمل؟ 🧠

```
┌─────────────────────────────────────────────────────────────┐
│                  مطالبة جديدة                               │
│                      │                                      │
│                      ▼                                      │
│           ┌─────────────────────┐                          │
│           │ البحث الدلالي      │                          │
│           │ (Cosine Similarity)│                          │
│           └─────────────────────┘                          │
│                      │                                      │
│         ┌───────────┴───────────┐                          │
│         │                       │                          │
│    تشابه > 85%            تشابه < 85%                     │
│         │                       │                          │
│         ▼                       ▼                          │
│   ┌──────────┐           ┌──────────┐                      │
│   │ استخدام │           │ تنفيذ   │                      │
│   │ النتيجة │           │ جديد    │                      │
│   │ المخزنة │           │ وحفظ    │                      │
│   └──────────┘           └──────────┘                      │
└─────────────────────────────────────────────────────────────┘
```

### إحصائيات التخزين المؤقت

```
┌─────────────────────────────────────────────────────────────┐
│ 📊 إحصائيات التخزين المؤقت                                 │
├─────────────────────────────────────────────────────────────┤
│ إجمالي الطلبات:     1,234                                  │
│ الإصابات (Hits):    892 (72.3%)                           │
│ الأخطاء (Misses):   342 (27.7%)                           │
│                                                             │
│ التوفير المقدر:     $45.67                                 │
│ زمن الاستجابة:      ~50ms (مقابل ~2000ms بدون تخزين)      │
│                                                             │
│ [🗑️ تنظيف التخزين]  [⚙️ إعدادات]  [📈 تقرير مفصل]        │
└─────────────────────────────────────────────────────────────┘
```

### إعدادات التخزين المؤقت

| الإعداد | الافتراضي | الوصف |
|---------|-----------|--------|
| عتبة التشابه | 85% | الحد الأدنى للتشابه |
| مدة الصلاحية | 7 أيام | مدة بقاء النتيجة |
| الحد الأقصى للحجم | 10,000 | عدد النتائج المخزنة |
| التنظيف التلقائي | كل 24 ساعة | جدولة التنظيف |

---

## النشر السحابي

### المنصات المدعومة ☁️

#### AWS (Amazon Web Services)

```yaml
# خطوات النشر على AWS
1. اذهب إلى /cloud-deployment
2. اختر AWS
3. أدخل بيانات الاعتماد:
   - AWS Access Key ID
   - AWS Secret Access Key
   - Region (مثل: us-east-1)
4. اختر نوع الخدمة:
   - EC2 (للتحكم الكامل)
   - ECS (للحاويات)
   - Lambda (بدون خادم)
5. اضغط "نشر"
```

#### Google Cloud Platform

```yaml
# خطوات النشر على GCP
1. اختر Google Cloud
2. ارفع ملف Service Account JSON
3. حدد المشروع
4. اختر نوع الخدمة:
   - Cloud Run (موصى به)
   - Compute Engine
   - App Engine
5. اضغط "نشر"
```

#### Vercel

```yaml
# خطوات النشر على Vercel
1. اختر Vercel
2. أدخل Vercel Token
3. حدد اسم المشروع
4. اختر المنطقة
5. اضغط "نشر"

# النشر التلقائي
- يتم تلقائياً عند كل push لـ main
```

#### Cloudflare

```yaml
# خطوات النشر على Cloudflare
1. اختر Cloudflare
2. أدخل API Token
3. حدد Account ID
4. اختر نوع الخدمة:
   - Workers
   - Pages
5. اضغط "نشر"
```

### مراقبة النشر

```
┌─────────────────────────────────────────────────────────────┐
│ 📊 حالة النشر                                               │
├─────────────────────────────────────────────────────────────┤
│ المنصة: AWS ECS                                             │
│ الحالة: 🟢 يعمل                                             │
│ الرابط: https://prompt-studio.aws.example.com              │
│                                                             │
│ المقاييس:                                                   │
│ ├─ وقت التشغيل: 99.9%                                      │
│ ├─ زمن الاستجابة: 120ms                                    │
│ ├─ الطلبات/ثانية: 45                                       │
│ └─ استخدام الذاكرة: 65%                                    │
│                                                             │
│ [📋 السجلات]  [🔄 إعادة النشر]  [⏹️ إيقاف]                │
└─────────────────────────────────────────────────────────────┘
```

---

## الإعدادات

### إعدادات عامة ⚙️

```
┌─────────────────────────────────────────────────────────────┐
│ الإعدادات العامة                                            │
├─────────────────────────────────────────────────────────────┤
│ اللغة: [العربية        ▼]                                  │
│ المظهر: [داكن          ▼]                                  │
│ حجم الخط: [متوسط       ▼]                                  │
│                                                             │
│ ☑️ تفعيل الإشعارات                                         │
│ ☑️ حفظ تلقائي                                              │
│ ☐ وضع المطور                                               │
└─────────────────────────────────────────────────────────────┘
```

### إعدادات API

```
┌─────────────────────────────────────────────────────────────┐
│ إعدادات مفاتيح API                                         │
├─────────────────────────────────────────────────────────────┤
│ Groq API Key:                                              │
│ [••••••••••••••••••••••••••••••••]  [👁️] [✏️]              │
│                                                             │
│ OpenAI API Key (للتخزين الدلالي):                          │
│ [••••••••••••••••••••••••••••••••]  [👁️] [✏️]              │
│                                                             │
│ [اختبار الاتصال]  [حفظ]                                    │
└─────────────────────────────────────────────────────────────┘
```

### إعدادات النماذج

```
┌─────────────────────────────────────────────────────────────┐
│ إعدادات النموذج الافتراضي                                   │
├─────────────────────────────────────────────────────────────┤
│ النموذج: [Llama 3.3 70B  ▼]                                │
│ Temperature: [0.7       ═══════●═══]                       │
│ Max Tokens: [2048      ▼]                                  │
│ Top P: [0.9            ═══════●═══]                        │
│                                                             │
│ ☑️ البث المباشر (Streaming)                                │
│ ☐ تسجيل جميع الطلبات                                       │
└─────────────────────────────────────────────────────────────┘
```

---

## أمثلة عملية

### مثال 1: إنشاء مطالبة لكتابة المقالات 📝

**الفكرة الأولية**:
```
أريد كتابة مقالات احترافية عن التقنية
```

**النتيجة بعد معالجة الوكلاء**:

```markdown
## System Prompt
أنت كاتب محتوى تقني محترف لديك خبرة 10 سنوات في الكتابة للمواقع التقنية الكبرى.
تتميز بأسلوب سهل وواضح يشرح المفاهيم المعقدة ببساطة. تلتزم بمعايير SEO
وتستخدم أمثلة عملية.

## Developer Instructions
- ابدأ بعنوان جذاب يحتوي على الكلمة المفتاحية
- استخدم فقرات قصيرة (3-4 جمل)
- أضف عناوين فرعية كل 200-300 كلمة
- استخدم قوائم نقطية للمعلومات المتسلسلة
- اختم بخلاصة وسؤال للتفاعل

## User Message
اكتب مقالاً عن {{topic}} موجهاً لـ {{audience}} بطول {{word_count}} كلمة.
الكلمات المفتاحية: {{keywords}}

## Context
المنصة: {{platform}}
نبرة الكتابة: {{tone}}
الهدف: {{goal}}
```

**استخدام المتغيرات**:
```json
{
  "topic": "الذكاء الاصطناعي في التعليم",
  "audience": "المعلمين وأولياء الأمور",
  "word_count": "1500",
  "keywords": "AI, تعليم, تقنية",
  "platform": "مدونة تعليمية",
  "tone": "ودود ومعلوماتي",
  "goal": "توعية وتثقيف"
}
```

---

### مثال 2: إنشاء مطالبة لتحليل البيانات 📊

**الفكرة الأولية**:
```
تحليل بيانات المبيعات واستخراج رؤى
```

**النتيجة النهائية**:

```markdown
## System Prompt
أنت محلل بيانات خبير متخصص في تحليل بيانات المبيعات والأعمال.
تستخدم منهجية علمية في التحليل وتقدم رؤى قابلة للتنفيذ.

## Developer Instructions
1. ابدأ بملخص تنفيذي (3-5 نقاط رئيسية)
2. قدم التحليل التفصيلي مع الأرقام
3. حدد الاتجاهات (trends) والأنماط
4. قارن بالفترات السابقة إن توفرت
5. قدم توصيات محددة وقابلة للقياس
6. استخدم جداول لعرض البيانات

## User Message
حلل بيانات المبيعات التالية واستخرج الرؤى الرئيسية:
{{sales_data}}

الفترة: {{period}}
المقارنة مع: {{comparison_period}}

## Context
نوع العمل: {{business_type}}
المنتجات الرئيسية: {{main_products}}
الأهداف: {{targets}}
```

**مثال على الاستخدام**:
```json
{
  "sales_data": "| الشهر | المبيعات | النمو |\n|-------|----------|-------|\n| يناير | 50,000 | +10% |",
  "period": "الربع الأول 2024",
  "comparison_period": "الربع الأول 2023",
  "business_type": "تجارة إلكترونية",
  "main_products": "إلكترونيات، ملابس",
  "targets": "نمو 20% سنوي"
}
```

---

### مثال 3: إنشاء مطالبة للترجمة الاحترافية 🌍

**النتيجة النهائية**:

```markdown
## System Prompt
أنت مترجم محترف معتمد متخصص في الترجمة بين العربية والإنجليزية.
تحافظ على المعنى والأسلوب والنبرة الأصلية مع مراعاة السياق الثقافي.

## Developer Instructions
- حافظ على بنية النص الأصلي
- استخدم المصطلحات المتخصصة الصحيحة
- راعِ الاختلافات الثقافية
- أضف ملاحظات للعبارات الاصطلاحية
- تجنب الترجمة الحرفية

## User Message
ترجم النص التالي من {{source_lang}} إلى {{target_lang}}:

{{text}}

## Context
المجال: {{domain}}
الجمهور المستهدف: {{target_audience}}
مستوى الرسمية: {{formality}}
```

---

### مثال 4: التعاون على مطالبة مع الفريق 👥

**سيناريو**: فريق تسويق يعمل على تحسين مطالبة لإنشاء محتوى إعلاني.

**الخطوات**:

1. **المدير** ينشئ جلسة تعاون جديدة
2. **يشارك الرابط** مع الفريق: `https://app/collaboration/abc123`
3. **أعضاء الفريق** ينضمون للجلسة
4. **التحرير المشترك**:
   - أحمد يحرر System Prompt
   - سارة تضيف أمثلة
   - محمد يراجع المتغيرات
5. **الحفظ** كقالب مشترك

```
┌─────────────────────────────────────────────────────────────┐
│ 📝 سجل التعديلات                                           │
├─────────────────────────────────────────────────────────────┤
│ 10:30 - أحمد: تعديل System Prompt                          │
│ 10:32 - سارة: إضافة مثال على الإعلان                       │
│ 10:35 - محمد: إضافة متغير brand_voice                      │
│ 10:38 - أحمد: تعديل Developer Instructions                │
│ 10:40 - الجميع: حفظ كقالب "إعلانات تسويقية v2"            │
└─────────────────────────────────────────────────────────────┘
```

---

## استكشاف الأخطاء

### مشاكل شائعة وحلولها

#### 1. خطأ في الاتصال بـ API

```
❌ خطأ: فشل الاتصال بـ API
```

**الحلول**:
```bash
# تحقق من مفتاح API
1. اذهب إلى الإعدادات > API
2. تأكد من صحة المفتاح
3. اضغط "اختبار الاتصال"

# تحقق من الاتصال بالإنترنت
ping api.groq.com

# تحقق من السجلات
npm run logs
```

#### 2. بطء في الاستجابة

```
⚠️ تحذير: زمن الاستجابة عالي
```

**الحلول**:
```bash
# تفعيل التخزين المؤقت
1. اذهب إلى الإعدادات > التخزين المؤقت
2. فعّل "التخزين الدلالي"
3. اضبط عتبة التشابه على 80%

# تقليل حجم الطلب
- قلل Max Tokens
- استخدم نموذج أصغر للمهام البسيطة
```

#### 3. خطأ في التعاون الحي

```
❌ خطأ: انقطع الاتصال بالجلسة
```

**الحلول**:
```bash
# إعادة الاتصال
1. اضغط "إعادة الاتصال"
2. إذا فشل، أغلق الصفحة وافتحها مجدداً

# تحقق من WebSocket
- تأكد من عدم حظر WebSocket بواسطة الجدار الناري
- جرب متصفحاً آخر
```

#### 4. فشل النشر السحابي

```
❌ خطأ: فشل النشر على AWS
```

**الحلول**:
```bash
# تحقق من الصلاحيات
1. تأكد من صلاحيات IAM الكافية
2. راجع سياسات الوصول

# تحقق من الحصص
- تأكد من عدم تجاوز حصص الخدمة
- راجع حدود المنطقة
```

---

## الأسئلة الشائعة

### عام

**س: هل يمكنني استخدام Prompt Studio بدون اتصال بالإنترنت؟**

ج: لا، يتطلب التطبيق اتصالاً بالإنترنت للوصول إلى نماذج الذكاء الاصطناعي. لكن يمكنك العمل على تحرير القوالب محلياً.

---

**س: ما هي النماذج المدعومة؟**

ج: حالياً ندعم:
- Llama 3.3 70B (عبر Groq)
- GPT-4 (عبر OpenAI)
- Claude 3 (عبر Anthropic)

---

**س: كيف يتم حساب التكلفة؟**

ج: التكلفة تعتمد على:
- عدد الرموز (tokens) في الطلب والاستجابة
- النموذج المستخدم
- التخزين المؤقت يقلل التكلفة بشكل كبير

---

### تقني

**س: كيف أضيف نموذج LLM جديد؟**

ج:
1. أضف مزود جديد في `server/llm-provider.ts`
2. عرّف واجهة المزود
3. أضف الإعدادات في الواجهة

---

**س: كيف أوسّع نظام الوكلاء؟**

ج:
1. أضف وكيلاً جديداً في `server/agents.ts`
2. عرّف الدور والمدخلات والمخرجات
3. دمجه في سير العمل

---

**س: كيف أخصص واجهة المستخدم؟**

ج:
1. المكونات في `client/src/components`
2. الأنماط في `tailwind.config.js`
3. المظاهر في `client/src/styles`

---

### الأمان

**س: كيف تُحمى مفاتيح API؟**

ج:
- تُخزن مشفرة في قاعدة البيانات
- لا تظهر في السجلات
- تُرسل عبر HTTPS فقط

---

**س: هل بياناتي آمنة في التعاون الحي؟**

ج:
- الاتصال مشفر عبر WSS
- الجلسات تنتهي تلقائياً
- لا نخزن محتوى الجلسات

---

## الدعم والتواصل

### الحصول على المساعدة

- 📖 **التوثيق**: `/docs`
- 💬 **المجتمع**: Discord/Slack
- 🐛 **الإبلاغ عن مشكلة**: GitHub Issues
- 📧 **الدعم الفني**: support@prompt-studio.com

### المساهمة

```bash
# استنساخ المشروع
git clone https://github.com/your-org/prompt-studio4.git

# إنشاء فرع للميزة
git checkout -b feature/my-feature

# إجراء التغييرات والاختبار
npm test

# إرسال Pull Request
git push origin feature/my-feature
```

---

## الملحقات

### ملحق أ: اختصارات لوحة المفاتيح

| الاختصار | الوظيفة |
|----------|---------|
| `Ctrl + S` | حفظ |
| `Ctrl + Enter` | تنفيذ المطالبة |
| `Ctrl + /` | تعليق/إلغاء تعليق |
| `Ctrl + Z` | تراجع |
| `Ctrl + Shift + Z` | إعادة |
| `Ctrl + D` | تكرار السطر |
| `Ctrl + F` | بحث |
| `Ctrl + H` | استبدال |
| `F1` | المساعدة |

### ملحق ب: متغيرات البيئة

```bash
# قاعدة البيانات
DATABASE_URL=postgresql://user:pass@localhost:5432/prompt_studio

# Redis
REDIS_URL=redis://localhost:6379

# مفاتيح API
GROQ_API_KEY=your_groq_key
OPENAI_API_KEY=your_openai_key

# إعدادات الخادم
PORT=5000
NODE_ENV=production

# إعدادات الأمان
SESSION_SECRET=your_secret
JWT_SECRET=your_jwt_secret
```

### ملحق ج: مخطط قاعدة البيانات

```sql
-- الجداول الرئيسية
templates          -- القوالب
techniques         -- التقنيات
prompts           -- المطالبات
runs              -- سجل التشغيل
users             -- المستخدمين
collaboration_sessions -- جلسات التعاون
semantic_cache    -- التخزين المؤقت
```

---

<div align="center">

**Prompt Studio 4** - منصة هندسة المطالبات المتقدمة

الإصدار 4.0.0 | © 2024

</div>

</div>
